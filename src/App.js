import React, { useState } from "react";
import { useQuery, useMutation } from "react-query";

function App() {
  // Main Component
  return (
    <div className="App">
      <h2>React Query</h2>
    </div>
  );
}

export default App;
